{"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"]},"docs":[{"location":"","title":"kevent-ai","text":"<p>kevent-ai is an AI inference job gateway for Kubernetes/Knative. It exposes an HTTP API that accepts file uploads, enqueues them as Kafka jobs, and returns results asynchronously \u2014 or synchronously for low-latency use cases.</p>"},{"location":"#quick-start","title":"Quick start","text":"<pre><code># Async job \u2014 fire and forget\ncurl -X POST https://your-gateway/jobs/audio \\\n  -F file=@audio.wav \\\n  -F model=whisper-large-v3\n\n# Poll for result\ncurl https://your-gateway/jobs/audio/{job_id}\n\n# Sync (OpenAI-compatible)\ncurl -X POST https://your-gateway/v1/audio/transcriptions \\\n  -F file=@audio.wav \\\n  -F model=whisper-large-v3\n</code></pre>"},{"location":"#two-operating-modes","title":"Two operating modes","text":"Mode Endpoint When to use Async <code>POST /jobs/{service_type}</code> Large files, batch workloads, fire-and-forget Sync <code>POST /v1/*</code> Low-latency, OpenAI-compatible clients <p>Sync mode has two sub-modes depending on config:</p> <ul> <li>Direct proxy \u2014 HTTP proxy to <code>inference_url</code> (JSON or multipart without <code>sync_topic</code>)</li> <li>Sync-over-Kafka \u2014 priority Kafka round-trip with keep-alive response (multipart + <code>sync_topic</code>)</li> </ul>"},{"location":"#components","title":"Components","text":"<p>Two independent Go binaries, two Docker images:</p> Component Image Entry point Gateway <code>ghcr.io/ia-generative/kevent-ai/gateway</code> <code>cmd/gateway/main.go</code> Relay <code>ghcr.io/ia-generative/kevent-ai/relay</code> <code>relay/cmd/relay/main.go</code> <p>The relay runs as a sidecar in each Knative InferenceService pod. It consumes Kafka jobs, calls the local model, and publishes results.</p>"},{"location":"#key-features","title":"Key features","text":"<ul> <li>Config-driven service registry \u2014 add a new model with a YAML block, no code change</li> <li>Hot-reload via <code>POST /-/reload</code> \u2014 update config without pod restart</li> <li>Priority routing \u2014 dedicated Kafka topic for SA/priority consumers</li> <li>Consumer tracking \u2014 link jobs to API consumers via a configurable header</li> <li>Prometheus metrics \u2014 requests, latency, Kafka errors, Redis operations, jobs by consumer</li> <li>OpenAPI 3.0 spec generated at runtime from the live registry</li> <li>AES-256-GCM at-rest encryption for S3 objects</li> </ul>"},{"location":"#links","title":"Links","text":"<ul> <li>Architecture overview</li> <li>Helm deployment</li> <li>Configuration reference</li> <li>Runbooks</li> <li>Changelog</li> </ul>"},{"location":"changelog/","title":"Changelog","text":"<p>All notable changes to this project are documented here. Format: Keep a Changelog. Versioning: each component is versioned independently \u2014 see tag conventions below.</p>"},{"location":"changelog/#tag-conventions","title":"Tag conventions","text":"Component Tag format Example Gateway (binary + Docker) <code>gateway/vX.Y.Z</code> <code>gateway/v0.2.5</code> Relay (binary + Docker) <code>relay/vX.Y.Z</code> <code>relay/v0.2.5</code> Helm chart auto-tagged by chart-releaser <code>kevent-gateway-0.2.0</code> <p>Note</p> <p>For the full changelog, see CHANGELOG.md on GitHub.</p>"},{"location":"architecture/overview/","title":"Architecture overview","text":"<p>kevent-ai consists of two independent components deployed separately:</p> <ul> <li>Gateway \u2014 HTTP server that accepts requests, routes them, and returns results</li> <li>Relay \u2014 sidecar that runs inside each Knative InferenceService pod, consuming Kafka jobs</li> </ul>"},{"location":"architecture/overview/#infrastructure-dependencies","title":"Infrastructure dependencies","text":""},{"location":"architecture/overview/#component-responsibilities","title":"Component responsibilities","text":""},{"location":"architecture/overview/#gateway","title":"Gateway","text":"<ul> <li>Accepts HTTP requests from clients</li> <li>Routes to the correct service based on <code>service_type</code>, <code>model</code>, and path</li> <li>Uploads input files to S3</li> <li>Persists job records in Redis (TTL 72h)</li> <li>Publishes <code>InputEvent</code> messages to Kafka</li> <li>Consumes <code>ResultEvent</code> messages and notifies clients</li> <li>Proxies sync requests directly to <code>inference_url</code></li> </ul>"},{"location":"architecture/overview/#relay","title":"Relay","text":"<ul> <li>Runs as a sidecar container in the InferenceService pod</li> <li>Consumes <code>InputEvent</code> from Kafka via KafkaSource (Knative Eventing)</li> <li>Downloads input from S3</li> <li>Calls the local inference model on <code>127.0.0.1:9000</code></li> <li>Uploads results to S3</li> <li>Publishes <code>ResultEvent</code> to Kafka</li> </ul>"},{"location":"architecture/overview/#data-flow","title":"Data flow","text":"<p>See Request flows for detailed sequence diagrams for each mode.</p>"},{"location":"architecture/overview/#service-registry","title":"Service registry","text":"<p>The gateway is entirely config-driven. See Service registry.</p>"},{"location":"architecture/overview/#priority-mechanism","title":"Priority mechanism","text":"<p>High-priority consumers can preempt async jobs. See Priority routing.</p>"},{"location":"architecture/priority/","title":"Priority routing","text":"<p>Priority routing lets SA (service account) consumers preempt normal async jobs, ensuring low-latency processing for premium workloads.</p>"},{"location":"architecture/priority/#how-it-works","title":"How it works","text":""},{"location":"architecture/priority/#gateway-side","title":"Gateway side","text":"<p>When a request carries the priority header (configurable via <code>server.priority_header</code>), the gateway publishes the <code>InputEvent</code> to the <code>priority_topic</code> instead of the normal <code>input_topic</code>.</p> <pre><code>Normal async:   Kafka jobs.&lt;model&gt;.input    \u2192 relay POST /\nPriority async: Kafka jobs.&lt;model&gt;.priority \u2192 relay POST /sync\n</code></pre>"},{"location":"architecture/priority/#relay-side","title":"Relay side","text":"<p>The relay's <code>POST /sync</code> handler sets <code>syncPriority++</code> for the duration of the job. Concurrent <code>POST /</code> (normal async) handlers check this counter:</p> <ul> <li><code>syncPriority &gt; 0</code> \u2192 return <code>503 Service Unavailable</code></li> <li>KafkaSource retries after <code>backoffDelay</code></li> <li>Once the priority job finishes, <code>syncPriority--</code> and async jobs resume</li> </ul> <p>This works across pod scale-out: each pod independently tracks its own priority state. No shared state across pods is needed.</p>"},{"location":"architecture/priority/#configuration","title":"Configuration","text":"<pre><code>server:\n  priority_header: \"X-Priority\"   # header name to check on incoming requests\n\nservices:\n  - type: audio\n    model: \"whisper-large-v3\"\n    priority_topic: jobs.whisper-large-v3.priority   # omit to disable priority routing\n    ...\n</code></pre>"},{"location":"architecture/priority/#knative-kafkasource-for-priority","title":"Knative KafkaSource for priority","text":"<p>Add a KafkaSource routing the priority topic to <code>POST /sync</code> on the relay:</p> <pre><code>apiVersion: sources.knative.dev/v1beta1\nkind: KafkaSource\nmetadata:\n  name: kafka-source-whisper-priority\nspec:\n  topics:\n    - jobs.whisper-large-v3.priority\n  consumerGroup: kevent-relay-priority\n  sink:\n    ref:\n      apiVersion: serving.knative.dev/v1\n      kind: Service\n      name: kevent-transcription\n  delivery:\n    backoffDelay: PT2S\n    backoffPolicy: exponential\n  extensions:\n    path: /sync\n</code></pre>"},{"location":"architecture/priority/#consumer-identification-and-isolation","title":"Consumer identification and isolation","text":"<p>When <code>server.consumer_header</code> is set (e.g. <code>X-Consumer-Username</code>, injected by APISIX after auth), the gateway:</p> <ol> <li>Stores <code>consumer_name</code> in the job record (Redis JSON)</li> <li>Maintains <code>consumer:{name}:jobs</code> sorted set (score = Unix timestamp, same TTL as job)</li> <li>Exposes <code>GET /jobs</code> to list a consumer's jobs (paginated, most-recent-first)</li> <li>Enforces ownership on <code>GET /jobs/{service_type}/{id}</code>: if the header is present, the job's <code>consumer_name</code> must match \u2014 returns <code>404</code> on mismatch</li> <li>Increments <code>kevent_jobs_by_consumer_total{mode, service_type, model, consumer}</code></li> </ol> <pre><code>server:\n  consumer_header: \"X-Consumer-Username\"   # set by APISIX after authentication\n</code></pre>"},{"location":"architecture/priority/#ownership-check-behaviour","title":"Ownership check behaviour","text":"<code>consumer_header</code> configured Header in request Result no \u2014 No check \u2014 auth-less deployments, all callers trusted yes absent No check \u2014 admin/internal calls bypass isolation yes present + matches job <code>200 OK</code> yes present + mismatch <code>404</code> \u2014 no information leak about other consumers' jobs"},{"location":"architecture/priority/#security-note","title":"Security note","text":"<p>Brute-force by job ID is not feasible \u2014 IDs are UUID v4 (2\u00b9\u00b2\u00b2 combinations). The ownership check adds defence-in-depth for authenticated deployments: even if a consumer somehow obtained another consumer's UUID, the gateway returns <code>404</code>.</p>"},{"location":"architecture/request-flows/","title":"Request flows","text":""},{"location":"architecture/request-flows/#async-mode","title":"Async mode","text":"<p><code>POST /jobs/{service_type}</code> \u2014 fire and forget, poll for result.</p> <p></p> <p>Client polling:</p> <pre><code>GET /jobs/{service_type}/{job_id}\n\u2192 { status: \"pending\" | \"completed\" | \"failed\", result: {...} }\n</code></pre>"},{"location":"architecture/request-flows/#sync-over-kafka-mode","title":"Sync-over-Kafka mode","text":"<p><code>POST /v1/*</code> multipart with <code>sync_topic</code> configured. The connection stays open; result is returned inline.</p> <p></p>"},{"location":"architecture/request-flows/#sync-direct-proxy-mode","title":"Sync direct proxy mode","text":"<p><code>POST /v1/*</code> JSON body, or multipart without <code>sync_topic</code>.</p> <pre><code>Client\n  \u2502  POST /v1/audio/transcriptions\n  \u2502  { \"model\": \"whisper-large-v3\", ... }\n  \u25bc\nGateway\n  \u2514\u2500\u2500 HTTP proxy \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u25ba InferenceService\n                                                 (inference_url + original path)\n                                                          \u2502\n                                                          \u25bc\n                                                 Response proxied back\n</code></pre>"},{"location":"architecture/request-flows/#routing-decision","title":"Routing decision","text":""},{"location":"architecture/service-registry/","title":"Service registry","text":"<p>The gateway is entirely config-driven. Adding a new model or service type requires only a YAML block in <code>config.yaml</code> \u2014 no Go code change.</p>"},{"location":"architecture/service-registry/#service-entry-fields","title":"Service entry fields","text":"<pre><code>services:\n  - type: audio                        # service type (used in /jobs/{service_type})\n    model: \"whisper-large-v3\"          # OpenAI \"model\" field value\n    default: true                      # fallback when request omits model field\n\n    # Sync / OpenAI-compatible\n    operations:\n      transcription:\n        - \"/v1/audio/transcriptions\"   # all paths indexed; first used for async\n      translation:\n        - \"/v1/audio/translations\"\n    inference_url: \"http://...\"        # backend base URL (path appended at runtime)\n    sync_topic: jobs.whisper.sync      # omit \u2192 use direct proxy for multipart\n\n    # Async / Kafka\n    input_topic: jobs.whisper.input\n    result_topic: jobs.whisper.results\n    priority_topic: jobs.whisper.priority  # optional \u2014 SA/priority consumers\n\n    # File validation\n    accepted_exts: [\".mp3\", \".wav\", \".m4a\", \".ogg\", \".flac\"]\n    max_file_size_mb: 500\n\n    # Backend authentication \u2014 sync-direct only (optional)\n    inference_headers:\n      Authorization: \"Bearer ${WHISPER_API_KEY}\"\n</code></pre>"},{"location":"architecture/service-registry/#model-resolution-order","title":"Model resolution order","text":"<p>When a request omits the <code>model</code> field:</p> <ol> <li>Single model registered for the type/path \u2192 auto-selected</li> <li>Model marked <code>default: true</code> \u2192 used as fallback</li> <li>Error listing available models</li> </ol>"},{"location":"architecture/service-registry/#operations","title":"Operations","text":"<p>The <code>operations</code> map replaces the old flat <code>openai_paths</code> list. Each key is an operation name; the value is a list of URL paths.</p> <ul> <li>All paths are indexed for sync routing (<code>POST /v1/*</code>)</li> <li>First path of the selected operation is embedded in the <code>InputEvent.InferenceURL</code> for async jobs</li> <li>The <code>operation</code> form field selects which operation to use for async submission (required when a model has multiple operations)</li> </ul> <pre><code># Async: specify operation when model has multiple\ncurl -X POST /jobs/audio \\\n  -F file=@audio.wav \\\n  -F operation=translation\n\n# Sync: operation is implicit from the URL path\ncurl -X POST /v1/audio/translations -F file=@audio.wav\n</code></pre>"},{"location":"architecture/service-registry/#multiple-models-per-type","title":"Multiple models per type","text":"<p>Multiple service entries may share the same <code>type</code> with different <code>model</code> values:</p> <pre><code>services:\n  - type: audio\n    model: \"whisper-large-v3\"\n    default: true\n    ...\n\n  - type: audio\n    model: \"whisper-large-v3-turbo\"\n    ...\n</code></pre> <p>The gateway routes by the <code>model</code> field in the request. The <code>default: true</code> flag designates the fallback.</p>"},{"location":"architecture/service-registry/#path-patterns-with-model","title":"Path patterns with <code>{model}</code>","text":"<p>Paths can embed the model name directly in the URL:</p> <pre><code>operations:\n  infer:\n    - \"/v2/models/{model}/infer\"\n</code></pre> <p>The gateway extracts the model name from the URL segment and routes accordingly. No <code>model</code> field is required in the request body.</p>"},{"location":"architecture/service-registry/#backend-authentication-inference_headers","title":"Backend authentication (<code>inference_headers</code>)","text":"<p>For sync-direct services whose backend requires authentication, add <code>inference_headers</code>:</p> <pre><code>- type: reranker\n  model: \"bge-reranker-v2-m3\"\n  operations:\n    rerank:\n      - \"/v1/rerank\"\n  inference_url: \"http://kevent-reranker-predictor.svc.cluster.local\"\n  inference_headers:\n    Authorization: \"Bearer ${RERANKER_API_KEY}\"\n</code></pre> <p>Headers are injected on every outgoing request to the backend. Values support <code>${VAR}</code> expansion. Config headers override client headers with the same name.</p> <p>Note</p> <p><code>inference_headers</code> only applies to sync-direct proxy. Async and sync-over-Kafka jobs run via the relay sidecar (local <code>127.0.0.1:9000</code>) and are unaffected.</p>"},{"location":"architecture/service-registry/#hot-reload","title":"Hot reload","text":"<p>The service registry is reloaded atomically via <code>POST /-/reload</code>. The HTTP router is swapped, Kafka consumers are reconciled (stopped for removed topics, started for new ones). Infrastructure (S3, Redis, Kafka connection) is not re-initialised.</p>"},{"location":"contributing/gitflow/","title":"Gitflow","text":""},{"location":"contributing/gitflow/#branch-model","title":"Branch model","text":"<p>Two long-lived branches:</p> Branch Environment Access <code>main</code> Production Protected \u2014 PR required <code>develop</code> Hors-prod (dev) Protected \u2014 PR required"},{"location":"contributing/gitflow/#feature-workflow","title":"Feature workflow","text":"<pre><code>develop\n   \u2502\n   \u251c\u2500\u2500 feat/my-feature      \u2190 branch from develop\n   \u2502        \u2502\n   \u2502        \u2514\u2500\u2500 PR \u2192 develop  \u2190 code review + CI\n   \u2502\n   \u251c\u2500\u2500 (PR merge)\n   \u2502\n   \u2514\u2500\u2500 release/vX.Y.Z       \u2190 branch from develop when ready\n            \u2502\n            \u2514\u2500\u2500 PR \u2192 main   \u2190 final QA, then tag\n</code></pre>"},{"location":"contributing/gitflow/#branch-naming","title":"Branch naming","text":"Type Pattern Example Feature <code>feat/&lt;short-name&gt;</code> <code>feat/redis-metrics</code> Bug fix <code>fix/&lt;short-name&gt;</code> <code>fix/kafka-timeout</code> Release prep <code>release/vX.Y.Z</code> <code>release/v0.6.0</code> Hotfix <code>hotfix/&lt;short-name&gt;</code> <code>hotfix/nil-pointer</code>"},{"location":"contributing/gitflow/#cicd-matrix","title":"CI/CD matrix","text":"Event CI Dev deploy Prod deploy Push to <code>feat/*</code>, <code>fix/*</code> Tests + vet \u2014 \u2014 PR to <code>develop</code> Tests + vet \u2014 \u2014 Push to <code>develop</code> Tests + vet <code>dev</code> image \u2014 Push to <code>release/**</code> Tests + vet <code>rc-&lt;sha&gt;</code> image \u2014 Push to <code>main</code> Tests + vet \u2014 \u2014 Tag <code>gateway/vX.Y.Z</code> Tests \u2014 <code>vX.Y.Z</code> image + GitHub Release Tag <code>relay/vX.Y.Z</code> Tests \u2014 <code>vX.Y.Z</code> image + GitHub Release Helm chart change on <code>main</code> \u2014 \u2014 Helm chart published to GitHub Pages"},{"location":"contributing/gitflow/#common-commands","title":"Common commands","text":"<pre><code># Start a feature\ngit checkout develop &amp;&amp; git pull\ngit checkout -b feat/my-feature\n\n# Open PR to develop\ngh pr create --base develop --title \"feat: ...\"\n\n# Create a release branch\ngit checkout develop &amp;&amp; git pull\ngit checkout -b release/v0.6.0\n\n# Open PR to main\ngh pr create --base main --title \"release: v0.6.0\"\n</code></pre>"},{"location":"contributing/gitflow/#hotfixes","title":"Hotfixes","text":"<p>Hotfixes branch from <code>main</code> and are merged back to both <code>main</code> and <code>develop</code>:</p> <pre><code>git checkout main &amp;&amp; git pull\ngit checkout -b hotfix/critical-fix\n\n# fix, commit, PR \u2192 main\ngh pr create --base main --title \"fix: critical fix\"\n\n# after merge: back-merge to develop\ngit checkout develop &amp;&amp; git pull\ngit merge main\ngit push origin develop\n</code></pre>"},{"location":"contributing/releasing/","title":"Releasing","text":""},{"location":"contributing/releasing/#pre-flight-checklist","title":"Pre-flight checklist","text":"<ul> <li>[ ] On branch <code>release/vX.Y.Z</code>, PR merged to <code>main</code></li> <li>[ ] <code>CHANGELOG.md</code> updated with the new version</li> <li>[ ] Helm <code>helm/gateway/Chart.yaml</code> version bumped (if chart changed)</li> <li>[ ] Helm <code>helm/gateway/values.yaml</code> <code>image.tag</code> updated</li> <li>[ ] <code>examples/inference-service.yaml</code> relay image tag updated (if relay changed)</li> </ul>"},{"location":"contributing/releasing/#tag-and-release","title":"Tag and release","text":"<pre><code># Ensure main is up-to-date\ngit checkout main &amp;&amp; git pull\n\n# Tag gateway\ngit tag gateway/vX.Y.Z\ngit push origin gateway/vX.Y.Z\n\n# Tag relay (if changed)\ngit tag relay/vX.Y.Z\ngit push origin relay/vX.Y.Z\n</code></pre> <p>CI will automatically:</p> <ol> <li>Build multi-arch Docker images</li> <li>Push to <code>ghcr.io/ia-generative/kevent-ai/gateway:vX.Y.Z</code></li> <li>Create a GitHub Release with binaries</li> </ol>"},{"location":"contributing/releasing/#back-merge-to-develop","title":"Back-merge to develop","text":"<p>After the release tag is pushed:</p> <pre><code>git checkout develop &amp;&amp; git pull\ngit merge main\ngit push origin develop\n</code></pre>"},{"location":"contributing/releasing/#image-locations","title":"Image locations","text":"Component Registry Gateway <code>ghcr.io/ia-generative/kevent-ai/gateway:vX.Y.Z</code> Relay <code>ghcr.io/ia-generative/kevent-ai/relay:vX.Y.Z</code> Dev (gateway) <code>ghcr.io/ia-generative/kevent-ai/gateway:dev</code> Dev (relay) <code>ghcr.io/ia-generative/kevent-ai/relay:dev</code>"},{"location":"contributing/releasing/#helm-chart","title":"Helm chart","text":"<p>The Helm chart is published automatically when <code>helm/**</code> changes on <code>main</code> (via <code>chart-releaser-action</code>). The chart registry is at:</p> <pre><code>https://ia-generative.github.io/kevent-ai\n</code></pre> <pre><code>helm repo add kevent https://ia-generative.github.io/kevent-ai\nhelm repo update\nhelm search repo kevent\n</code></pre>"},{"location":"deployment/configuration/","title":"Configuration reference","text":"<p>Configuration is loaded from <code>config.yaml</code> (or <code>$CONFIG_PATH</code>). Values of the form <code>${VAR}</code> or <code>${VAR:-default}</code> are expanded from the environment.</p>"},{"location":"deployment/configuration/#server","title":"Server","text":"<pre><code>server:\n  addr: \":8080\"\n  read_timeout: 120s\n  write_timeout: 0s        # 0 = no timeout (recommended for long sync jobs)\n  idle_timeout: 120s\n  consumer_header: \"\"      # header name for consumer identification (e.g. X-Consumer-Username)\n  priority_header: \"\"      # header name for priority routing (e.g. X-Priority)\n</code></pre>"},{"location":"deployment/configuration/#consumer_header","title":"<code>consumer_header</code>","text":"<p>When set to a non-empty header name (e.g. <code>X-Consumer-Username</code>, typically injected by APISIX after authentication):</p> <ul> <li>The consumer name is stored in the job record (<code>consumer_name</code> field in Redis).</li> <li>A Redis sorted set <code>consumer:{name}:jobs</code> is maintained per consumer (score = creation timestamp, same TTL as the job). Used by <code>GET /jobs</code>.</li> <li><code>GET /jobs/{service_type}/{id}</code> enforces ownership: if the header is present in the request, <code>job.consumer_name</code> must match \u2014 returns <code>404</code> on mismatch (no information leak about other consumers' jobs).</li> <li><code>kevent_jobs_by_consumer_total{mode, service_type, model, consumer}</code> is incremented per submission.</li> </ul> <p>Leave empty in deployments without upstream authentication \u2014 no behaviour change, zero overhead.</p>"},{"location":"deployment/configuration/#priority_header","title":"<code>priority_header</code>","text":"<p>When set and a request carries this header, the job is published to <code>services[].priority_topic</code> instead of <code>input_topic</code>. The relay processes priority-topic jobs via <code>POST /sync</code>, which sets <code>syncPriority++</code> and defers normal async jobs for its duration.</p> <p>Leave empty to disable priority routing.</p>"},{"location":"deployment/configuration/#kafka","title":"Kafka","text":"<pre><code>kafka:\n  brokers:\n    - \"kafka:9092\"\n  consumer_group: \"kevent-gateway\"\n  sasl:\n    mechanism: \"SCRAM-SHA-512\"   # PLAIN | SCRAM-SHA-256 | SCRAM-SHA-512\n    username: \"${KAFKA_USERNAME}\"\n    password: \"${KAFKA_PASSWORD}\"\n  tls:\n    enabled: true\n    ca_cert_path: \"/etc/ssl/certs/kafka-ca.crt\"\n</code></pre>"},{"location":"deployment/configuration/#s3","title":"S3","text":"<pre><code>s3:\n  endpoint: \"${S3_ENDPOINT:-https://s3.fr-par.scw.cloud}\"\n  region: \"${S3_REGION:-fr-par}\"\n  access_key: \"${S3_ACCESS_KEY}\"\n  secret_key: \"${S3_SECRET_KEY}\"\n  bucket: \"${S3_BUCKET:-kevent-jobs}\"\n</code></pre>"},{"location":"deployment/configuration/#encryption","title":"Encryption","text":"<pre><code>encryption:\n  key: \"${ENCRYPTION_KEY:-}\"   # hex-encoded 32-byte AES-256 key; empty = disabled\n</code></pre> <p>Generate a key:</p> <pre><code>openssl rand -hex 32\n</code></pre> <p>Warning</p> <p>The encryption key must be identical on all gateway and relay instances.</p>"},{"location":"deployment/configuration/#redis","title":"Redis","text":"<pre><code>redis:\n  addr: \"${REDIS_ADDR:-redis:6379}\"\n  password: \"${REDIS_PASSWORD:-}\"\n  db: 0\n  job_ttl_hours: 72\n</code></pre>"},{"location":"deployment/configuration/#services","title":"Services","text":"<pre><code>services:\n  - type: audio                            # service type\n    model: \"whisper-large-v3\"              # OpenAI model field\n    default: true                          # fallback when model omitted\n\n    # Sync routing\n    operations:\n      transcription:\n        - \"/v1/audio/transcriptions\"\n      translation:\n        - \"/v1/audio/translations\"\n    inference_url: \"http://backend:80\"     # base URL; original path appended\n    sync_topic: \"jobs.whisper.sync\"        # omit \u2192 direct proxy for multipart\n\n    # Async routing\n    input_topic: \"jobs.whisper.input\"\n    result_topic: \"jobs.whisper.results\"\n    priority_topic: \"jobs.whisper.priority\"   # optional\n\n    # File validation\n    accepted_exts: [\".mp3\", \".wav\", \".m4a\", \".ogg\", \".flac\"]\n    max_file_size_mb: 500\n\n    # Backend authentication (sync-direct only, optional)\n    inference_headers:\n      Authorization: \"Bearer ${RERANKER_API_KEY}\"\n      X-Api-Key: \"${BACKEND_KEY}\"\n\n    # Swagger spec (optional)\n    swagger_url: \"https://example.com/openapi.json\"\n    swagger_headers:\n      Authorization: \"Bearer ${TOKEN}\"\n      Accept: \"application/octet-stream\"\n</code></pre>"},{"location":"deployment/configuration/#field-reference","title":"Field reference","text":"Field Required Default Description <code>type</code> yes \u2014 Service type, used in <code>/jobs/{service_type}</code> <code>model</code> no <code>\"\"</code> OpenAI model field value for routing <code>default</code> no <code>false</code> Fallback model when request omits <code>model</code> <code>operations</code> no <code>{}</code> Map of operation name \u2192 URL paths <code>inference_url</code> no <code>\"\"</code> Backend base URL for direct proxy <code>sync_topic</code> no <code>\"\"</code> Priority Kafka topic for sync-over-Kafka <code>input_topic</code> no <code>\"\"</code> Kafka input topic for async jobs <code>result_topic</code> no <code>\"\"</code> Kafka result topic for async jobs <code>priority_topic</code> no <code>\"\"</code> Kafka topic for priority async jobs <code>accepted_exts</code> no any Allowed file extensions (e.g. <code>.mp3</code>) <code>max_file_size_mb</code> no <code>100</code> Max upload size in MB <code>inference_headers</code> no <code>{}</code> HTTP headers injected on every sync-direct proxy request <code>swagger_url</code> no <code>\"\"</code> URL to fetch an OpenAPI spec from <code>swagger_headers</code> no <code>{}</code> HTTP headers for <code>swagger_url</code> fetch"},{"location":"deployment/configuration/#inference_headers","title":"<code>inference_headers</code>","text":"<p>Arbitrary HTTP headers injected on every request forwarded to the inference backend. Only applies to the sync-direct proxy flow (JSON requests and multipart without <code>sync_topic</code>). Has no effect on async or sync-over-Kafka jobs.</p> <ul> <li>Header values support <code>${VAR}</code> / <code>${VAR:-default}</code> env expansion \u2014 store credentials in environment variables, not in plain config.</li> <li>Config headers override any header with the same name sent by the client.</li> </ul> <pre><code># Typical use cases:\ninference_headers:\n  Authorization: \"Bearer ${RERANKER_API_KEY}\"   # Bearer token\n  X-Api-Key: \"${BACKEND_KEY}\"                   # custom API key header\n  apikey: \"${BACKEND_KEY}\"                      # APISIX-style key\n</code></pre>"},{"location":"deployment/configuration/#hot-reload","title":"Hot reload","text":"<p><code>POST /-/reload</code> re-reads the config file and atomically swaps the router. Kafka consumers are reconciled (stopped for removed topics, started for new ones). S3, Redis, and Kafka connections are not re-initialised.</p> <p>The <code>configmap-reload</code> sidecar can trigger this automatically on ConfigMap changes.</p>"},{"location":"deployment/helm/","title":"Helm chart","text":"<p>The gateway is packaged as a Helm chart and published to GitHub Pages.</p>"},{"location":"deployment/helm/#install","title":"Install","text":"<pre><code>helm repo add kevent https://ia-generative.github.io/kevent-ai\nhelm repo update\n\nhelm install kevent-gateway kevent/kevent-gateway -f values.yaml\n</code></pre>"},{"location":"deployment/helm/#upgrade","title":"Upgrade","text":"<pre><code>helm upgrade kevent-gateway kevent/kevent-gateway -f values.yaml\n</code></pre>"},{"location":"deployment/helm/#install-from-local-sources","title":"Install from local sources","text":"<pre><code>helm upgrade --install kevent-gateway ./helm/gateway -f values.yaml\n</code></pre>"},{"location":"deployment/helm/#minimal-valuesyaml","title":"Minimal <code>values.yaml</code>","text":"<pre><code>image:\n  tag: \"v0.5.3\"\n\nconfig:\n  s3:\n    endpoint: \"https://s3.fr-par.scw.cloud\"\n    region: \"fr-par\"\n    bucket: \"my-kevent-jobs\"\n  redis:\n    addr: \"redis:6379\"\n  kafka:\n    brokers:\n      - \"kafka:9092\"\n\nsecrets:\n  s3AccessKey: \"my-access-key\"\n  s3SecretKey: \"my-secret-key\"\n</code></pre>"},{"location":"deployment/helm/#redis-ha","title":"Redis HA","text":"<p>The chart deploys Redis with Redis-HA (HAProxy front-end) by default. Redis is required for job state and sync-over-Kafka pub/sub.</p>"},{"location":"deployment/helm/#configmap-hot-reload","title":"ConfigMap hot reload","text":"<p>The chart supports <code>configmap-reload</code> sidecar to trigger <code>POST /-/reload</code> automatically when the ConfigMap changes:</p> <pre><code>configReloader:\n  enabled: true\n  image: ghcr.io/jimmidyson/configmap-reload:v0.14.0\n</code></pre>"},{"location":"deployment/helm/#ingress","title":"Ingress","text":"<pre><code>ingress:\n  enabled: true\n  className: nginx\n  host: kevent.example.com\n  tls:\n    enabled: true\n    secretName: kevent-tls\n</code></pre>"},{"location":"deployment/helm/#extra-environment-variables","title":"Extra environment variables","text":"<pre><code>extraEnvVars:\n  - name: ENCRYPTION_KEY\n    valueFrom:\n      secretKeyRef:\n        name: kevent-encryption\n        key: key\n</code></pre>"},{"location":"deployment/helm/#apply-strimzi-kafka-users","title":"Apply Strimzi Kafka users","text":"<pre><code>kubectl apply -f examples/kafka-users.yaml -n &lt;kafka-namespace&gt;\n</code></pre> <p>See <code>examples/kafka-users.yaml</code> in the repository root. Adjust the <code>strimzi.io/cluster</code> label and namespace to match your Strimzi installation.</p>"},{"location":"deployment/helm/#apply-kafkasources","title":"Apply KafkaSources","text":"<pre><code>kubectl apply -f examples/kafka-sources.yaml\n</code></pre> <p>See <code>examples/kafka-sources.yaml</code> in the repository root. Replace the <code>&lt;placeholder&gt;</code> values (broker address, secret names, InferenceService name) before applying.</p>"},{"location":"deployment/helm/#apply-inferenceservice","title":"Apply InferenceService","text":"<pre><code>kubectl apply -f examples/inference-service.yaml\n</code></pre> <p>See <code>examples/inference-service.yaml</code> in the repository root. Replace all <code>&lt;placeholder&gt;</code> values (model PVC, image, broker address, secret names) before applying.</p>"},{"location":"deployment/kafka/","title":"Kafka &amp; SASL configuration","text":""},{"location":"deployment/kafka/#broker","title":"Broker","text":"<p>kevent supports any Kafka broker reachable from the cluster. Configure the address in <code>kafka.brokers</code>.</p>"},{"location":"deployment/kafka/#sasltls-configuration","title":"SASL/TLS configuration","text":"<pre><code>kafka:\n  brokers:\n    - \"kafka-bootstrap.&lt;kafka-namespace&gt;.svc.cluster.local:9093\"\n  sasl:\n    mechanism: \"SCRAM-SHA-512\"  # or PLAIN, SCRAM-SHA-256\n    username: \"${KAFKA_USERNAME}\"\n    password: \"${KAFKA_PASSWORD}\"\n  tls:\n    enabled: true\n    ca_cert_path: \"/etc/ssl/certs/kafka-ca.crt\"\n</code></pre>"},{"location":"deployment/kafka/#strimzi-kafkausers","title":"Strimzi KafkaUsers","text":"<p>Apply in the namespace where your Strimzi Kafka cluster is managed:</p> <pre><code>kubectl apply -f examples/kafka-users.yaml -n &lt;kafka-namespace&gt;\n</code></pre> <p>See <code>examples/kafka-users.yaml</code> in the repository root for the full manifest.</p>"},{"location":"deployment/kafka/#kevent-gateway","title":"<code>kevent-gateway</code>","text":"<ul> <li>Write/Read on <code>jobs.*</code> topics</li> <li>Read on <code>kevent-gateway*</code> consumer groups</li> </ul>"},{"location":"deployment/kafka/#kevent-relay","title":"<code>kevent-relay</code>","text":"<ul> <li>Read on <code>jobs.*</code> topics</li> <li>Write on <code>jobs.*</code> topics</li> <li>Read + Describe + Delete on <code>inference-*</code> groups</li> </ul> <p>Delete ACL required</p> <p>The <code>Delete</code> ACL on <code>inference-*</code> groups is required by the Knative controller for ConsumerGroup finalization. Without it, KafkaSources will fail to clean up.</p>"},{"location":"deployment/kafka/#secret-hygiene","title":"Secret hygiene","text":"<p>Strimzi-generated secrets must not have trailing newlines in values. The Knative KafkaSource controller does exact string comparison on <code>sasl-type</code> \u2014 a <code>\\n</code> suffix causes:</p> <pre><code>[protocol SASL_SSL] unsupported SASL mechanism\n</code></pre> <p>Verify:</p> <pre><code>kubectl get secret kevent-relay-kafka -n default \\\n  -o jsonpath='{.data.sasl-type}' | base64 -d | xxd\n</code></pre> <p>The output must end with <code>5332 302d 3531 32</code> (<code>S</code> <code>C</code> <code>R</code> <code>A</code> <code>M</code> <code>-</code> <code>S</code> <code>H</code> <code>A</code> <code>-</code> <code>5</code> <code>1</code> <code>2</code>) \u2014 no <code>0a</code> byte at the end.</p>"},{"location":"deployment/kafka/#kafkasources","title":"KafkaSources","text":"<p>Each service type requires two KafkaSources in the InferenceService namespace:</p> Source Topic Relay endpoint Purpose <code>kafka-source-{model}</code> <code>jobs.{model}.input</code> <code>POST /</code> Normal async jobs <code>kafka-source-{model}-sync</code> <code>jobs.{model}.sync</code> <code>POST /sync</code> Sync-over-Kafka (priority) <code>kafka-source-{model}-priority</code> <code>jobs.{model}.priority</code> <code>POST /sync</code> SA priority async jobs <p>The <code>sync</code> and <code>priority</code> sources route to <code>POST /sync</code>, which sets <code>syncPriority++</code> and defers normal async processing.</p>"},{"location":"deployment/kafka/#topic-naming","title":"Topic naming","text":"Topic Producer Consumer <code>jobs.{model}.input</code> Gateway (async submit) Relay via KafkaSource <code>jobs.{model}.sync</code> Gateway (sync-over-Kafka) Relay via KafkaSource <code>jobs.{model}.priority</code> Gateway (priority submit) Relay via KafkaSource <code>jobs.{model}.results</code> Relay Gateway ConsumerManager"},{"location":"runbooks/KeventGatewayHighErrorRate/","title":"KeventGatewayHighErrorRate","text":"<p>Severity: warning / critical Component: Gateway</p>"},{"location":"runbooks/KeventGatewayHighErrorRate/#meaning","title":"Meaning","text":"<p>More than 5% (warning) or 20% (critical) of gateway requests are returning HTTP 5xx errors over the last 5 minutes.</p>"},{"location":"runbooks/KeventGatewayHighErrorRate/#impact","title":"Impact","text":"<p>Clients are receiving errors. Async job submissions or sync inference calls are failing.</p>"},{"location":"runbooks/KeventGatewayHighErrorRate/#diagnosis","title":"Diagnosis","text":"<pre><code># Error rate by mode and service_type\nrate(kevent_requests_total{status=~\"5..\"}[5m])\n\n# Check gateway pod logs\nkubectl logs -l app.kubernetes.io/name=kevent-gateway -n &lt;namespace&gt; --tail=100\n\n# Check which status codes are returned\nsum by (status, mode, service_type) (\n  rate(kevent_requests_total[5m])\n)\n</code></pre> <p>Common causes: - S3 unavailable \u2192 check <code>kevent_s3_errors_total</code> - Kafka unavailable \u2192 check <code>kevent_kafka_publish_errors_total</code> - Redis unavailable \u2192 gateway logs will show connection errors - Upstream inference service returning errors (sync mode)</p>"},{"location":"runbooks/KeventGatewayHighErrorRate/#mitigation","title":"Mitigation","text":"<ol> <li>Check S3, Kafka, and Redis connectivity from the gateway pod</li> <li>If S3 is down: async jobs will fail at upload \u2014 check bucket and credentials</li> <li>If Kafka is down: async submissions fail at enqueue \u2014 check broker reachability</li> <li>If Redis is down: sync-over-Kafka jobs will stall \u2014 check Redis HA haproxy endpoint</li> <li>Scale the gateway deployment if the issue is resource exhaustion</li> </ol>"},{"location":"runbooks/KeventGatewayKafkaPublishErrors/","title":"KeventGatewayKafkaPublishErrors","text":"<p>Severity: critical Component: Gateway</p>"},{"location":"runbooks/KeventGatewayKafkaPublishErrors/#meaning","title":"Meaning","text":"<p>The gateway failed to publish one or more input events to Kafka in the last 5 minutes. Affected jobs are immediately marked <code>failed</code> in Redis.</p>"},{"location":"runbooks/KeventGatewayKafkaPublishErrors/#impact","title":"Impact","text":"<p>Async job submissions (<code>POST /jobs/{service_type}</code>) return 500. Files are uploaded to S3 but never processed.</p>"},{"location":"runbooks/KeventGatewayKafkaPublishErrors/#diagnosis","title":"Diagnosis","text":"<pre><code># Total publish errors by topic\nincrease(kevent_kafka_publish_errors_total[5m])\n\n# Gateway logs\nkubectl logs -l app.kubernetes.io/name=kevent-gateway -n &lt;namespace&gt; | grep \"kafka publish failed\"\n\n# Check broker connectivity from the gateway pod\nkubectl exec -it &lt;gateway-pod&gt; -n &lt;namespace&gt; -- nc -zv &lt;broker-host&gt; &lt;broker-port&gt;\n</code></pre> <p>Common causes: - Kafka broker unreachable (network policy, broker restart) - SASL credentials expired or rotated - Topic does not exist (<code>AllowAutoTopicCreation: false</code> \u2014 topic must be pre-created) - Broker disk full / under-replicated</p>"},{"location":"runbooks/KeventGatewayKafkaPublishErrors/#mitigation","title":"Mitigation","text":"<ol> <li>Verify broker address in gateway config (<code>kafka.brokers</code>)</li> <li>Check SASL credentials \u2014 rotate secret if needed</li> <li>Confirm topic exists: <code>kafka-topics.sh --list --bootstrap-server &lt;broker&gt;</code></li> <li>Check Strimzi KafkaUser ACLs (<code>examples/kafka-users.yaml</code>) \u2014 <code>Write</code> on <code>jobs.*</code> required</li> <li>Failed jobs remain in Redis with status <code>failed</code> for TTL duration \u2014 clients must resubmit</li> </ol>"},{"location":"runbooks/KeventGatewayS3Errors/","title":"KeventGatewayS3Errors","text":"<p>Severity: warning Component: Gateway</p>"},{"location":"runbooks/KeventGatewayS3Errors/#meaning","title":"Meaning","text":"<p>The gateway encountered S3 errors (upload, get, or delete) in the last 5 minutes.</p>"},{"location":"runbooks/KeventGatewayS3Errors/#impact","title":"Impact","text":"<ul> <li>Upload errors: async job submissions fail at the file storage step (500 returned to client)</li> <li>Get errors: completed job results cannot be fetched (<code>GET /jobs/{type}/{id}</code> returns incomplete data)</li> <li>Delete errors: orphaned files accumulate in S3 (no functional impact, storage cost)</li> </ul>"},{"location":"runbooks/KeventGatewayS3Errors/#diagnosis","title":"Diagnosis","text":"<pre><code># Errors by operation\nsum by (operation) (increase(kevent_s3_errors_total[5m]))\n\n# Gateway logs\nkubectl logs -l app.kubernetes.io/name=kevent-gateway -n &lt;namespace&gt; | grep -E \"s3 (upload|result fetch) failed\"\n\n# Check S3 reachability\nkubectl exec -it &lt;gateway-pod&gt; -n &lt;namespace&gt; -- curl -I &lt;s3-endpoint&gt;\n</code></pre> <p>Common causes: - S3 credentials expired or incorrect - Bucket does not exist or wrong region configured - Network connectivity issue to the S3 endpoint - S3 service outage</p>"},{"location":"runbooks/KeventGatewayS3Errors/#mitigation","title":"Mitigation","text":"<ol> <li>Verify S3 credentials in the secret (<code>S3_ACCESS_KEY</code>, <code>S3_SECRET_KEY</code>)</li> <li>Check endpoint and region in gateway config</li> <li>Confirm bucket exists and the credentials have read/write access</li> <li>If encryption is enabled, verify <code>ENCRYPTION_KEY</code> matches between gateway and relay</li> </ol>"},{"location":"runbooks/KeventGatewaySyncJobsInFlightHigh/","title":"KeventGatewaySyncJobsInFlightHigh","text":"<p>Severity: warning Component: Gateway</p>"},{"location":"runbooks/KeventGatewaySyncJobsInFlightHigh/#meaning","title":"Meaning","text":"<p>More than the configured threshold of sync-over-Kafka connections are open simultaneously in the gateway. Each open connection is a client waiting for an inference result.</p>"},{"location":"runbooks/KeventGatewaySyncJobsInFlightHigh/#impact","title":"Impact","text":"<p>High in-flight count indicates saturation. New sync requests may time out if relay pods cannot keep up. Knative may be scaling up pods (normal), or the autoscaler is lagging.</p>"},{"location":"runbooks/KeventGatewaySyncJobsInFlightHigh/#diagnosis","title":"Diagnosis","text":"<pre><code># Current in-flight connections\nkevent_sync_jobs_in_flight\n\n# Sync wait duration distribution (check for long waits)\nhistogram_quantile(0.95, rate(kevent_sync_wait_duration_seconds_bucket[5m]))\n\n# Check relay pod count\nkubectl get pods -n &lt;namespace&gt; | grep predictor\n\n# Check relay sync priority (1 = sync job active on that pod)\nkevent_relay_sync_priority\n</code></pre> <p>Common causes: - Inference is slow (large files, complex models) \u2014 normal spike during heavy load - Relay pods not scaling fast enough (check Knative autoscaler config) - Relay pods crashing mid-job \u2014 check relay logs</p>"},{"location":"runbooks/KeventGatewaySyncJobsInFlightHigh/#mitigation","title":"Mitigation","text":"<ol> <li>If inference is legitimately slow: increase <code>scaleTarget</code> or reduce <code>containerConcurrency</code> on the KServe InferenceService to trigger faster scale-out</li> <li>If relay pods are failing: check relay logs and Knative pod events</li> <li>If the spike is transient: no action needed \u2014 Knative will scale out and drain the queue</li> </ol>"},{"location":"runbooks/KeventRelayInferenceSlow/","title":"KeventRelayInferenceSlow","text":"<p>Severity: warning Component: Relay sidecar</p>"},{"location":"runbooks/KeventRelayInferenceSlow/#meaning","title":"Meaning","text":"<p>The 95th percentile inference duration exceeds the configured threshold (default: 120 s). This measures the time the relay spends calling the local inference model.</p>"},{"location":"runbooks/KeventRelayInferenceSlow/#impact","title":"Impact","text":"<ul> <li>Sync-over-Kafka clients are held open longer, increasing <code>kevent_sync_jobs_in_flight</code></li> <li>Async jobs complete later than expected</li> <li>Risk of Knative <code>timeoutSeconds</code> being hit, causing relay context cancellation</li> </ul>"},{"location":"runbooks/KeventRelayInferenceSlow/#diagnosis","title":"Diagnosis","text":"<pre><code># p50 / p95 / p99 inference duration by service_type\nhistogram_quantile(0.95,\n  sum by (service_type, le) (\n    rate(kevent_relay_inference_duration_seconds_bucket[10m])\n  )\n)\n\n# Input file size distribution (large files = expected slow inference)\nhistogram_quantile(0.95,\n  sum by (service_type, le) (\n    rate(kevent_relay_input_size_bytes_bucket[10m])\n  )\n)\n\n# GPU utilization (if available via DCGM exporter)\n# DCGM_FI_DEV_GPU_UTIL\n</code></pre> <p>Common causes: - Large input files (audio &gt; 10 min, high-resolution images) - GPU contention (multiple concurrent jobs on one pod) - Model cold start after scale-from-zero - Inference model degraded (OOM pressure, throttling)</p>"},{"location":"runbooks/KeventRelayInferenceSlow/#mitigation","title":"Mitigation","text":"<ol> <li>If caused by large inputs: expected behaviour \u2014 consider raising the alerting threshold or use <code>max_file_size_mb</code> to limit input size</li> <li>If GPU contention: lower <code>containerConcurrency</code> on the InferenceService to prevent concurrent GPU usage</li> <li>If cold start: configure Knative <code>minReplicas: 1</code> to avoid scale-to-zero for latency-sensitive services</li> <li>Check model pod resource limits \u2014 GPU memory pressure can slow inference significantly</li> </ol>"},{"location":"runbooks/KeventRelayJobFailureRate/","title":"KeventRelayJobFailureRate","text":"<p>Severity: warning / critical Component: Relay sidecar</p>"},{"location":"runbooks/KeventRelayJobFailureRate/#meaning","title":"Meaning","text":"<p>More than 10% (warning) or 30% (critical) of jobs processed by relay sidecars are failing.</p>"},{"location":"runbooks/KeventRelayJobFailureRate/#impact","title":"Impact","text":"<p>Async jobs complete with status <code>failed</code>. The gateway notifies clients via webhook (if configured) with the error. Sync-over-Kafka jobs return an error in the HTTP response body.</p>"},{"location":"runbooks/KeventRelayJobFailureRate/#diagnosis","title":"Diagnosis","text":"<pre><code># Failure rate by service_type\nsum by (service_type) (rate(kevent_relay_jobs_total{status=\"failed\"}[5m]))\n  /\nsum by (service_type) (rate(kevent_relay_jobs_total[5m]))\n\n# Relay pod logs (check for inference errors)\nkubectl logs -l serving.kserve.io/inferenceservice=&lt;name&gt; -c kserve-container -n &lt;namespace&gt; --tail=100\n\n# Relay S3 errors (download failures)\nincrease(kevent_relay_s3_errors_total[5m])\n\n# Inference duration \u2014 timeout issues?\nhistogram_quantile(0.95, rate(kevent_relay_inference_duration_seconds_bucket[5m]))\n</code></pre> <p>Common causes: - Inference model returning non-2xx (malformed input, OOM, model crash) - S3 download failure (input file missing or corrupted) - Inference timeout (<code>inference.timeout</code> in relay config) - GPU OOM \u2014 model pod restarted mid-job</p>"},{"location":"runbooks/KeventRelayJobFailureRate/#mitigation","title":"Mitigation","text":"<ol> <li>Check relay logs for the specific error (<code>inference error</code>, <code>s3 download failed</code>)</li> <li>If the model is crashing: check GPU memory usage, reduce <code>containerConcurrency</code></li> <li>If timeouts: increase <code>inference.timeout</code> in relay config, or investigate model performance</li> <li>If S3 errors: check S3 credentials and encryption key consistency between gateway and relay</li> <li>Resubmit failed jobs via <code>POST /jobs/{service_type}</code> \u2014 results are cleaned up after TTL (72h default)</li> </ol>"},{"location":"runbooks/KeventRelayS3Errors/","title":"KeventRelayS3Errors","text":"<p>Severity: warning Component: Relay sidecar</p>"},{"location":"runbooks/KeventRelayS3Errors/#meaning","title":"Meaning","text":"<p>Relay sidecars encountered S3 errors (get/put/delete) in the last 5 minutes.</p>"},{"location":"runbooks/KeventRelayS3Errors/#impact","title":"Impact","text":"<ul> <li>Get errors: relay cannot download the input file \u2192 job marked <code>failed</code></li> <li>Put errors: inference result cannot be uploaded \u2192 job marked <code>failed</code>, result lost</li> <li>Delete errors: input file not cleaned up after processing (storage cost, no functional impact)</li> </ul>"},{"location":"runbooks/KeventRelayS3Errors/#diagnosis","title":"Diagnosis","text":"<pre><code># Errors by operation\nsum by (operation) (increase(kevent_relay_s3_errors_total[5m]))\n\n# Relay logs\nkubectl logs -l serving.kserve.io/inferenceservice=&lt;name&gt; -c kserve-container -n &lt;namespace&gt; \\\n  | grep -E \"s3|S3\"\n\n# Check if gateway S3 errors also firing (shared credentials issue)\nincrease(kevent_s3_errors_total[5m])\n</code></pre> <p>Common causes: - S3 credentials not injected into relay pod (<code>S3_ACCESS_KEY</code>, <code>S3_SECRET_KEY</code> env vars) - Encryption key mismatch between gateway (writes) and relay (reads) \u2014 <code>ENCRYPTION_KEY</code> must be identical - Input file deleted before relay could download it (TTL issue or duplicate job) - S3 endpoint unreachable from the inference pod's network namespace</p>"},{"location":"runbooks/KeventRelayS3Errors/#mitigation","title":"Mitigation","text":"<ol> <li>Verify S3 env vars are set in the KServe ServingRuntime/InferenceService manifest</li> <li>Confirm <code>ENCRYPTION_KEY</code> is the same secret referenced in both gateway and relay deployments</li> <li>Check network policies \u2014 inference pods must reach the S3 endpoint</li> <li>If input files are disappearing prematurely: check gateway job TTL (<code>job_ttl_hours</code>) vs inference duration</li> </ol>"}]}