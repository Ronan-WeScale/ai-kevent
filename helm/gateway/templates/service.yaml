apiVersion: v1
kind: Service
metadata:
  name: {{ include "kevent-gateway.fullname" . }}
  labels:
    {{- include "kevent-gateway.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "kevent-gateway.selectorLabels" . | nindent 4 }}
